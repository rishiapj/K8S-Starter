apiVersion: v1
kind: Service
metadata:
  name: mvc-app-service
spec:
  type: LoadBalancer
  selector:
    app: mvc-app
  ports:
    - protocol: TCP
      port: 8088
      targetPort: 8080
      nodePort: 3010




